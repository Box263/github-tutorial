name: CI
on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main", "feature/**" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI running"
